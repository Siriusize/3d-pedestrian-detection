# 3d-pedestrian-detection

To run the demo by 

python detect.py --image_folder ./data1/samples/

The project requires pytorch and tensorflow, which is listed in requirements.

The weights are in the folder weights. Run download_weights.sh before run the whole project.